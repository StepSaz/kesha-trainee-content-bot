# M3/M4/M5 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add normalized memory (M3), self-callbacks (M4), and Telegram bot digest trigger + comment reactions (M5) to kesha-trainee-content-bot.

**Architecture:** New `src/lib/memory.ts` owns blob-backed MemoryEntry storage and callback matching; `pipeline.ts` consumes it for dedup and callback injection; `kesha-boss-background.mts` gains `/digest` command with inline-keyboard approval and Stepan-only comment reply handling.

**Tech Stack:** Node.js 22 + TypeScript ESM, Netlify Functions v2 (`@netlify/functions`), `@netlify/blobs`, `@anthropic-ai/sdk`, Vitest

---

## File Map

| File | Action | Responsibility |
|---|---|---|
| `src/lib/memory.ts` | **create** | MemoryEntry type, loadMemory, appendMemory, findCallbacks |
| `src/lib/__tests__/memory.test.ts` | **create** | Unit tests for memory module |
| `src/lib/pipeline.ts` | modify | PipelineOptions: publishedTopics→memoryEntries; dedup in selectTopics; callback injection in generatePost |
| `src/lib/__tests__/pipeline.test.ts` | modify | Add memory mock, update option shapes |
| `src/config/kesha-persona.txt` | modify | Add callback stylistic move to optional штрихи section |
| `netlify/functions/kesha-post-background.mts` | modify | Replace published-topics blob with loadMemory/appendMemory |
| `netlify/functions/kesha-test-background.mts` | modify | Same as above |
| `src/lib/telegram.ts` | modify | Add `replyToMessageId` option to sendMessage |
| `netlify/functions/kesha-boss-background.mts` | modify | /digest flow, digest callback handler, comment reaction handler |

---

## Task 1: memory.ts — loadMemory + appendMemory

**Files:**
- Create: `src/lib/memory.ts`
- Create: `src/lib/__tests__/memory.test.ts`

- [ ] **Step 1.1: Write failing tests for loadMemory and appendMemory**

Create `src/lib/__tests__/memory.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';

const mockGet = vi.fn();
const mockSetJSON = vi.fn();

vi.mock('@netlify/blobs', () => ({
  getStore: () => ({ get: mockGet, setJSON: mockSetJSON }),
}));

import { loadMemory, appendMemory, type MemoryEntry } from '../memory.js';

beforeEach(() => vi.clearAllMocks());

const entry = (title: string, daysAgo = 0): MemoryEntry => ({
  url: 'https://example.com',
  title,
  publishedAt: new Date(Date.now() - daysAgo * 86_400_000).toISOString(),
  postId: null,
});

describe('loadMemory', () => {
  it('returns [] when both blobs absent', async () => {
    mockGet.mockResolvedValue(null);
    expect(await loadMemory()).toEqual([]);
    expect(mockSetJSON).not.toHaveBeenCalled();
  });

  it('returns memory-v1 when present', async () => {
    const entries = [entry('Claude 4.7 release')];
    mockGet.mockResolvedValue(entries);
    expect(await loadMemory()).toEqual(entries);
  });

  it('migrates published-topics when memory-v1 absent', async () => {
    const oldTopics = ['1. [T1] Claude 4.7 релиз (hn)\n2. [T2] OpenAI DevDay (web)'];
    mockGet
      .mockResolvedValueOnce(null)        // memory-v1 miss
      .mockResolvedValueOnce(oldTopics);  // published-topics hit
    const result = await loadMemory();
    expect(result).toHaveLength(2);
    expect(result[0].title).toBe('Claude 4.7 релиз');
    expect(result[1].title).toBe('OpenAI DevDay');
    expect(result[0].url).toBe('');
    expect(result[0].postId).toBeNull();
    expect(mockSetJSON).toHaveBeenCalledWith('memory-v1', result);
  });

  it('returns [] when both blobs absent (no published-topics)', async () => {
    mockGet.mockResolvedValue(null);
    expect(await loadMemory()).toEqual([]);
    expect(mockSetJSON).not.toHaveBeenCalled();
  });
});

describe('appendMemory', () => {
  it('appends entries to existing memory', async () => {
    const existing = [entry('Old topic')];
    mockGet.mockResolvedValue(existing);
    const newEntry = entry('New topic');
    await appendMemory([newEntry]);
    expect(mockSetJSON).toHaveBeenCalledWith('memory-v1', [...existing, newEntry]);
  });

  it('starts from empty when blob absent', async () => {
    mockGet.mockResolvedValue(null);
    const newEntry = entry('First ever');
    await appendMemory([newEntry]);
    expect(mockSetJSON).toHaveBeenCalledWith('memory-v1', [newEntry]);
  });

  it('trims to 40 entries FIFO when over limit', async () => {
    const existing: MemoryEntry[] = Array.from({ length: 40 }, (_, i) =>
      entry(`Entry ${i}`));
    mockGet.mockResolvedValue(existing);
    await appendMemory([entry('New')]);
    const saved = mockSetJSON.mock.calls[0][1] as MemoryEntry[];
    expect(saved).toHaveLength(40);
    expect(saved[0].title).toBe('Entry 1');   // Entry 0 dropped
    expect(saved[39].title).toBe('New');
  });
});
```

- [ ] **Step 1.2: Run tests to verify they fail**

```bash
npx vitest run src/lib/__tests__/memory.test.ts
```

Expected: `Error: Cannot find module '../memory.js'`

- [ ] **Step 1.3: Implement memory.ts**

Create `src/lib/memory.ts`:

```typescript
import { getStore } from '@netlify/blobs';

export interface MemoryEntry {
  url: string;
  title: string;
  publishedAt: string;
  postId: number | null;
}

const BLOB_KEY = 'memory-v1';
const MAX_ENTRIES = 40;

const TOPIC_REGEX = /\d+\.\s+\[T\d\]\s+(.+?)\s+\((?:hn|web)\)/g;

export async function loadMemory(): Promise<MemoryEntry[]> {
  try {
    const store = getStore('kesha');
    const existing = await store.get(BLOB_KEY, { type: 'json' }) as MemoryEntry[] | null;
    if (existing) return existing;

    // One-time migration from published-topics prose format
    const oldTopics = await store.get('published-topics', { type: 'json' }) as string[] | null;
    if (!oldTopics || oldTopics.length === 0) return [];

    const now = new Date().toISOString();
    const entries: MemoryEntry[] = [];
    for (const topicStr of oldTopics) {
      for (const match of topicStr.matchAll(TOPIC_REGEX)) {
        entries.push({ url: '', title: match[1], publishedAt: now, postId: null });
      }
    }
    if (entries.length > 0) await store.setJSON(BLOB_KEY, entries);
    return entries;
  } catch {
    return [];
  }
}

export async function appendMemory(entries: MemoryEntry[]): Promise<void> {
  try {
    const store = getStore('kesha');
    const existing = await store.get(BLOB_KEY, { type: 'json' }) as MemoryEntry[] | null ?? [];
    const updated = [...existing, ...entries].slice(-MAX_ENTRIES);
    await store.setJSON(BLOB_KEY, updated);
  } catch {
    // non-fatal: memory write failure does not block publishing
  }
}
```

- [ ] **Step 1.4: Run tests to verify they pass**

```bash
npx vitest run src/lib/__tests__/memory.test.ts
```

Expected: all `loadMemory` and `appendMemory` tests PASS

- [ ] **Step 1.5: Commit**

```bash
git add src/lib/memory.ts src/lib/__tests__/memory.test.ts
git commit -m "feat(memory): add loadMemory + appendMemory with published-topics migration (M3)"
```

---

## Task 2: memory.ts — findCallbacks

**Files:**
- Modify: `src/lib/memory.ts`
- Modify: `src/lib/__tests__/memory.test.ts`

- [ ] **Step 2.1: Add failing tests for findCallbacks**

Append to `src/lib/__tests__/memory.test.ts`:

```typescript
import { findCallbacks } from '../memory.js';

const weeksAgo = (n: number): string =>
  new Date(Date.now() - n * 7 * 24 * 60 * 60 * 1000).toISOString();

describe('findCallbacks', () => {
  it('returns [] for empty memory', () => {
    expect(findCallbacks(['OpenAI launch'], [])).toEqual([]);
  });

  it('returns entry matching by keyword (2-8 weeks ago)', () => {
    const e: MemoryEntry = { url: '', title: 'OpenAI DevDay launch', publishedAt: weeksAgo(3), postId: null };
    const result = findCallbacks(['OpenAI announcement'], [e]);
    expect(result).toHaveLength(1);
    expect(result[0].title).toBe('OpenAI DevDay launch');
  });

  it('ignores entries < 2 weeks old (dedup zone)', () => {
    const e: MemoryEntry = { url: '', title: 'OpenAI DevDay launch', publishedAt: weeksAgo(1), postId: null };
    expect(findCallbacks(['OpenAI announcement'], [e])).toEqual([]);
  });

  it('ignores entries > 8 weeks old', () => {
    const e: MemoryEntry = { url: '', title: 'OpenAI DevDay launch', publishedAt: weeksAgo(9), postId: null };
    expect(findCallbacks(['OpenAI announcement'], [e])).toEqual([]);
  });

  it('ignores entries when no keyword (≥5 chars) overlaps', () => {
    const e: MemoryEntry = { url: '', title: 'Google Maps update', publishedAt: weeksAgo(3), postId: null };
    expect(findCallbacks(['OpenAI announcement'], [e])).toEqual([]);
  });

  it('caps result at 3 matches', () => {
    const entries: MemoryEntry[] = Array.from({ length: 5 }, (_, i) => ({
      url: '', title: `Claude release ${i}`, publishedAt: weeksAgo(3), postId: null,
    }));
    expect(findCallbacks(['Claude update'], entries).length).toBeLessThanOrEqual(3);
  });
});
```

- [ ] **Step 2.2: Run tests to verify they fail**

```bash
npx vitest run src/lib/__tests__/memory.test.ts
```

Expected: `findCallbacks` tests fail with `is not a function`

- [ ] **Step 2.3: Implement findCallbacks**

Append to `src/lib/memory.ts`:

```typescript
const MS_PER_WEEK = 7 * 24 * 60 * 60 * 1000;

export function findCallbacks(newTitles: string[], memory: MemoryEntry[]): MemoryEntry[] {
  const now = Date.now();
  const results: MemoryEntry[] = [];

  for (const entry of memory) {
    const ageWeeks = (now - new Date(entry.publishedAt).getTime()) / MS_PER_WEEK;
    if (ageWeeks < 2 || ageWeeks > 8) continue;

    const entryWords = entry.title.toLowerCase().split(/\s+/).filter(w => w.length >= 5);
    if (entryWords.length === 0) continue;

    const matched = newTitles.some(title => {
      const titleWords = title.toLowerCase().split(/\s+/).filter(w => w.length >= 5);
      return titleWords.some(tw => entryWords.some(ew => ew.includes(tw) || tw.includes(ew)));
    });

    if (matched) results.push(entry);
    if (results.length === 3) break;
  }

  return results;
}
```

- [ ] **Step 2.4: Run all memory tests**

```bash
npx vitest run src/lib/__tests__/memory.test.ts
```

Expected: all tests PASS

- [ ] **Step 2.5: Commit**

```bash
git add src/lib/memory.ts src/lib/__tests__/memory.test.ts
git commit -m "feat(memory): add findCallbacks — keyword match 2-8 weeks back (M4)"
```

---

## Task 3: pipeline.ts — M3 dedup via memoryEntries

**Files:**
- Modify: `src/lib/pipeline.ts`
- Modify: `src/lib/__tests__/pipeline.test.ts`

- [ ] **Step 3.1: Add memory mock and update PipelineOptions in pipeline tests**

In `src/lib/__tests__/pipeline.test.ts`, add after the existing `vi.mock` block at the top:

```typescript
vi.mock('../memory.js', () => ({
  findCallbacks: vi.fn().mockReturnValue([]),
}));
```

Add to imports section (after existing imports):

```typescript
import { findCallbacks } from '../memory.js';
const mockFindCallbacks = vi.mocked(findCallbacks);
```

Add `mockFindCallbacks.mockReturnValue([]);` inside `beforeEach`.

- [ ] **Step 3.2: Run existing pipeline tests — should still pass**

```bash
npx vitest run src/lib/__tests__/pipeline.test.ts
```

Expected: all existing tests PASS (no changes to pipeline.ts yet)

- [ ] **Step 3.3: Update PipelineOptions and selectTopics in pipeline.ts**

In `src/lib/pipeline.ts`, add import at the top (after existing imports):

```typescript
import { type MemoryEntry, findCallbacks } from './memory.js';
```

Replace the `PipelineOptions` interface (lines 64-67):

```typescript
export interface PipelineOptions {
  memoryEntries?: MemoryEntry[];
  previousIntros?: string[];
}
```

Replace the `selectTopics` function signature (line 162):

```typescript
async function selectTopics(
  hnContext: string,
  webContext: string,
  cfg: PipelineConfig,
  memoryEntries?: MemoryEntry[]
): Promise<SelectedTopics> {
```

Replace the `dedupBlock` construction inside `selectTopics` (lines 199-204, the block starting with `const sliced = publishedTopics`):

```typescript
  const recentMs = 8 * 7 * 24 * 60 * 60 * 1000;
  const recent = (memoryEntries ?? []).filter(
    e => Date.now() - new Date(e.publishedAt).getTime() < recentMs
  );
  const dedupBlock = recent.length > 0
    ? `\n\nТЕМЫ ИЗ ПОСЛЕДНИХ ПОСТОВ (НЕ повторяй эти же события - даже если они снова в фиде):\n${
        recent.map(e => `- ${e.title}${e.url ? ` (${e.url})` : ''}`).join('\n')
      }\n\nПравила анти-дублирования:\n- Если новость о ТОМ ЖЕ событии (тот же продукт, тот же релиз, та же сделка) - пропусти.\n- Если есть ЗНАЧИМОЕ продолжение (новые цифры, реакция рынка, отозвали/расширили) - можно включить, но обозначь как развитие, не как анонс.\n- Если нет нового угла - пропусти, даже если событие крупное.`
    : '';
```

Update the call to `selectTopics` inside `generatePipelinePost` (line 342):

```typescript
const selectedTopics = await selectTopics(hnContext, webContext, cfg, options.memoryEntries);
```

- [ ] **Step 3.4: Run pipeline tests**

```bash
npx vitest run src/lib/__tests__/pipeline.test.ts
```

Expected: all tests PASS

- [ ] **Step 3.5: Run full test suite**

```bash
npx vitest run
```

Expected: all tests PASS

- [ ] **Step 3.6: Commit**

```bash
git add src/lib/pipeline.ts src/lib/__tests__/pipeline.test.ts
git commit -m "feat(pipeline): replace publishedTopics string[] with memoryEntries for structured dedup (M3)"
```

---

## Task 4: pipeline.ts — M4 callbacks + persona

**Files:**
- Modify: `src/lib/pipeline.ts`
- Modify: `src/config/kesha-persona.txt`

- [ ] **Step 4.1: Update generatePost to accept and inject memoryEntries**

In `src/lib/pipeline.ts`, update the `generatePost` function signature (line ~216):

```typescript
async function generatePost(
  hnContext: string,
  webContext: string,
  selectedTopics: SelectedTopics,
  cfg: PipelineConfig,
  previousIntros?: string[],
  memoryEntries?: MemoryEntry[]
): Promise<string> {
```

Inside `generatePost`, after the `introsBlock` construction (after line ~249), add:

```typescript
  const callbackMatches = findCallbacks(
    selectedTopics.topics.map(t => t.title),
    memoryEntries ?? []
  );
  const callbackBlock = callbackMatches.length > 0
    ? `\n\nCALLBACK CONTEXT (используй по желанию — только если органично вписывается в пост):\n${
        callbackMatches.map(e => {
          const weeksAgo = Math.round(
            (Date.now() - new Date(e.publishedAt).getTime()) / (7 * 24 * 60 * 60 * 1000)
          );
          const ago = weeksAgo === 1 ? '1 неделю' : `${weeksAgo} недели`;
          return `- "${e.title}" упоминался ${ago} назад — можно сослаться как на развитие истории`;
        }).join('\n')
      }`
    : '';
```

In the `callClaude` call inside `generatePost`, append `callbackBlock` to the `userMessage`:

```typescript
    userMessage: `Сегодня ${date}, ${time} по Варшаве.\n\nКонтекст из Hacker News:\n${hnContext}\n\nКонтекст из веб-поиска:\n${webContext}\n\nОтобранные темы:\n${topicsProse}${sparseNote}${topicNote}${introsBlock}${callbackBlock}\n\nНапиши пост для Telegram-канала @psyreq в своём стиле.`,
```

Update the call to `generatePost` inside `generatePipelinePost` (line ~354):

```typescript
const draft = await generatePost(hnContext, webContext, selectedTopics, cfg, options.previousIntros, options.memoryEntries);
```

- [ ] **Step 4.2: Add callback stylistic move to kesha-persona.txt**

In `src/config/kesha-persona.txt`, find line 71 (end of the ОПЦИОНАЛЬНЫЕ ШТРИХИ section, before the blank line and "ПРОВЕРКА ЛОГИКИ ВЫВОДА"). Add after the existing two bullet points:

```
- Callback к прошлой истории: если в блоке CALLBACK CONTEXT есть подходящее совпадение, можно одной фразой упомянуть что следишь за развитием истории — «пару недель назад только гадал, выйдет ли — вышло» или «помню, обсуждали X несколько недель назад — вот что стало». Без надуманности: если совпадение неочевидно, пропусти.
```

- [ ] **Step 4.3: Run tests**

```bash
npx vitest run
```

Expected: all tests PASS

- [ ] **Step 4.4: Commit**

```bash
git add src/lib/pipeline.ts src/config/kesha-persona.txt
git commit -m "feat(pipeline): inject callback context for matching memory entries (M4)"
```

---

## Task 5: kesha-post-background.mts + kesha-test-background.mts — use memory.ts

**Files:**
- Modify: `netlify/functions/kesha-post-background.mts`
- Modify: `netlify/functions/kesha-test-background.mts`

- [ ] **Step 5.1: Update kesha-post-background.mts**

In `netlify/functions/kesha-post-background.mts`:

Add import (after the existing imports):

```typescript
import { loadMemory, appendMemory, type MemoryEntry } from '../../src/lib/memory.js';
```

Remove the `formatSelectedTopics` import (it's no longer needed here). Update the import line from pipeline.js:

```typescript
import { generatePipelinePost, extractIntro, type PipelineOptions, type PipelineResult } from '../../src/lib/pipeline.js';
```

Replace lines 31-33 (published-topics read):

```typescript
  const memoryEntries = await loadMemory();
  const previousIntros = (await store.get('previous-intros', { type: 'json' }) as string[] | null) ?? [];
  const pipelineOptions: PipelineOptions = { memoryEntries, previousIntros };
```

Replace lines 107-108 (published-topics write) with:

```typescript
      const newEntries: MemoryEntry[] = pipelineResult.selectedTopics.topics.map(t => ({
        url: t.sourceUrl,
        title: t.title,
        publishedAt: new Date().toISOString(),
        postId: sendResult.messageId ?? null,
      }));
      await appendMemory(newEntries);
```

- [ ] **Step 5.2: Update kesha-test-background.mts**

In `netlify/functions/kesha-test-background.mts`:

Add import (after existing imports):

```typescript
import { loadMemory } from '../../src/lib/memory.js';
```

Replace lines 44-46 (published-topics read in pipeline branch):

```typescript
      const memoryEntries = await loadMemory();
      const previousIntros = (await store.get('previous-intros', { type: 'json' }) as string[] | null) ?? [];
      const testOptions: PipelineOptions = { memoryEntries, previousIntros };
```

Note: kesha-test-background does NOT call `appendMemory` — test runs do not update long-term memory.

- [ ] **Step 5.3: Run full test suite**

```bash
npx vitest run
```

Expected: all tests PASS

- [ ] **Step 5.4: Commit**

```bash
git add netlify/functions/kesha-post-background.mts netlify/functions/kesha-test-background.mts
git commit -m "feat(cron): wire memory.ts into post and test background functions (M3)"
```

---

## Task 6: telegram.ts — add replyToMessageId

**Files:**
- Modify: `src/lib/telegram.ts`

- [ ] **Step 6.1: Update sendMessage to support replyToMessageId**

In `src/lib/telegram.ts`, update `sendMessage` options type:

```typescript
export async function sendMessage(
  chatId: string,
  text: string,
  options?: { replyMarkup?: InlineKeyboard | null; replyToMessageId?: number }
): Promise<SendResult> {
```

Inside the function body, where `body` is constructed, add after the `replyMarkup` block:

```typescript
    if (options?.replyToMessageId) {
      body.reply_to_message_id = options.replyToMessageId;
    }
```

- [ ] **Step 6.2: Run tests**

```bash
npx vitest run
```

Expected: all tests PASS

- [ ] **Step 6.3: Commit**

```bash
git add src/lib/telegram.ts
git commit -m "feat(telegram): add replyToMessageId option to sendMessage (M5 prereq)"
```

---

## Task 7: kesha-boss-background.mts — /digest command + approval flow

**Files:**
- Modify: `netlify/functions/kesha-boss-background.mts`

- [ ] **Step 7.1: Add PendingDigest type and imports**

In `netlify/functions/kesha-boss-background.mts`, add to imports:

```typescript
import { generatePipelinePost, extractIntro, type PipelineResult } from '../../src/lib/pipeline.js';
import { loadMemory, appendMemory, type MemoryEntry } from '../../src/lib/memory.js';
```

Add the `PendingDigest` interface after the existing `PendingPreview` interface:

```typescript
interface PendingDigest {
  chatId: string;
  progressMessageId: number;
  post: string;
  selectedTopics: PipelineResult['selectedTopics'];
  newIntros: string[];
  createdAt: string;
}
```

- [ ] **Step 7.2: Add handleDigest function**

Add before `export default`:

```typescript
async function handleDigest(message: TelegramMessage): Promise<void> {
  const chatId = String(message.chat.id);
  const config = readBossConfig();

  if (!config.allowed_user_ids.includes(message.from.id)) {
    await sendMessage(chatId, 'Только для начальника 🐤');
    return;
  }

  const progressResult = await sendMessage(chatId, '⏳ генерирую дайджест... (~60-90 сек)');
  if (!progressResult.success || !progressResult.messageId) return;
  const progressMessageId = progressResult.messageId;

  const store = getStore('kesha');

  try {
    const memoryEntries = await loadMemory();
    const previousIntros = (await store.get('previous-intros', { type: 'json' }) as string[] | null) ?? [];

    const result = await generatePipelinePost({ memoryEntries, previousIntros });

    if (!result.success || !result.post) {
      await editMessageText(chatId, progressMessageId,
        `❌ Пайплайн упал: ${(result.errors ?? []).join(', ')}`);
      return;
    }

    const newIntro = extractIntro(result.post);
    const newIntros = [...previousIntros, newIntro].slice(-10);

    const pending: PendingDigest = {
      chatId,
      progressMessageId,
      post: result.post,
      selectedTopics: result.selectedTopics,
      newIntros,
      createdAt: new Date().toISOString(),
    };
    await store.setJSON('pending-digest', pending);

    const keyboard: InlineKeyboard = {
      inline_keyboard: [[
        { text: '🧪 В тест', callback_data: 'digest_test' },
        { text: '📢 В прод', callback_data: 'digest_prod' },
        { text: '❌ Отмена', callback_data: 'digest_cancel' },
      ]],
    };

    await editMessageText(chatId, progressMessageId, '✅ Готово, пост ниже — выбери куда отправить:');
    await sendMessage(chatId, result.post, { replyMarkup: keyboard });
  } catch (err) {
    await editMessageText(chatId, progressMessageId, `❌ Ошибка: ${String(err)}`);
  }
}
```

- [ ] **Step 7.3: Add handleDigestCallback function**

Add after `handleDigest`:

```typescript
async function handleDigestCallback(callbackQuery: TelegramCallbackQuery): Promise<void> {
  const callbackQueryId = callbackQuery.id;
  const chatId = String(callbackQuery.message?.chat.id ?? '');
  const messageId = callbackQuery.message?.message_id ?? 0;
  const data = callbackQuery.data ?? '';

  const store = getStore('kesha');
  const pending = await store.get('pending-digest', { type: 'json' }) as PendingDigest | null;

  if (!pending) {
    await answerCallbackQuery(callbackQueryId);
    await editMessageText(chatId, messageId, '⏰ Дайджест устарел — запусти /digest снова.', { replyMarkup: null });
    return;
  }

  if (data === 'digest_cancel') {
    await store.delete('pending-digest');
    await answerCallbackQuery(callbackQueryId);
    await editMessageText(chatId, messageId, '❌ Отменено.', { replyMarkup: null });
    return;
  }

  const targetChatId = data === 'digest_test'
    ? process.env.TELEGRAM_TEST_CHAT_ID!
    : process.env.TELEGRAM_CHAT_ID!;

  const sendResult = await sendToChannel(pending.post, targetChatId);
  await store.delete('pending-digest');
  await answerCallbackQuery(callbackQueryId);

  if (!sendResult.success) {
    await editMessageText(chatId, messageId,
      `❌ Ошибка отправки: ${sendResult.error}`, { replyMarkup: null });
    return;
  }

  // Update memory and intros after successful publish
  const newEntries: MemoryEntry[] = pending.selectedTopics.topics.map(t => ({
    url: t.sourceUrl,
    title: t.title,
    publishedAt: new Date().toISOString(),
    postId: sendResult.messageId ?? null,
  }));
  await appendMemory(newEntries);
  await store.setJSON('previous-intros', pending.newIntros);

  const label = data === 'digest_test' ? 'тест' : 'прод';
  await editMessageText(chatId, messageId,
    `✅ Отправлено в ${label}: t.me/psyreq/${sendResult.messageId}`, { replyMarkup: null });
}
```

- [ ] **Step 7.4: Update the main router in kesha-boss-background.mts**

Replace the current routing block at the bottom of the file:

```typescript
  if (update.message?.text?.match(/^\/boss/)) {
    await handleCommand(update.message);
  } else if (update.callback_query) {
    await handleCallback(update.callback_query);
  }
```

With:

```typescript
  const msg = update.message;
  const cq = update.callback_query;

  if (cq) {
    const data = cq.data ?? '';
    if (data.startsWith('digest_')) {
      await handleDigestCallback(cq);
    } else {
      await handleCallback(cq);
    }
  } else if (msg?.text?.match(/^\/digest/)) {
    await handleDigest(msg);
  } else if (msg?.text?.match(/^\/boss/)) {
    await handleCommand(msg);
  }
```

- [ ] **Step 7.5: Run tests**

```bash
npx vitest run
```

Expected: all tests PASS

- [ ] **Step 7.6: Commit**

```bash
git add netlify/functions/kesha-boss-background.mts
git commit -m "feat(boss): add /digest command with inline-keyboard approve/cancel flow (M5)"
```

---

## Task 8: kesha-boss-background.mts — comment reactions

**Files:**
- Modify: `netlify/functions/kesha-boss-background.mts`

- [ ] **Step 8.1: Add comment intent parser and handleCommentReply**

In `netlify/functions/kesha-boss-background.mts`, add at the top of the file (after imports):

```typescript
import { callClaude } from '../../src/lib/claude.js';
```

Add the intent parser and reply handler before `export default`:

```typescript
type CommentIntent = 'expand' | 'explain' | 'compare' | 'freeform';

function parseCommentIntent(text: string): CommentIntent {
  const t = text.toLowerCase();
  if (/расширь|подробнее|больше/.test(t)) return 'expand';
  if (/объясни|что значит|что такое/.test(t)) return 'explain';
  if (/сравни|vs\b|versus/.test(t)) return 'compare';
  return 'freeform';
}

async function handleCommentReply(message: TelegramMessage): Promise<void> {
  const config = readBossConfig();
  if (!config.allowed_user_ids.includes(message.from.id)) return;

  const replyToId = (message as any).reply_to_message?.message_id as number | undefined;
  if (!replyToId) return;

  const store = getStore('kesha');
  const rateKey = `comment-rate:${replyToId}`;
  const currentCount = (await store.get(rateKey, { type: 'json' }) as number | null) ?? 0;
  if (currentCount >= 3) return;
  await store.setJSON(rateKey, currentCount + 1);

  const intent = parseCommentIntent(message.text ?? '');
  const postText = (message as any).reply_to_message?.text as string ?? '';

  const intentInstructions: Record<CommentIntent, string> = {
    expand: 'Степан просит развернуть тему подробнее. Напиши 2-3 абзаца, углубись в детали.',
    explain: 'Степан просит объяснить проще. Объясни как для умного нетехнического человека.',
    compare: 'Степан просит сравнение. Сравни кратко — что лучше, хуже, в каком контексте.',
    freeform: 'Степан написал комментарий к посту. Ответь по делу, в своём стиле стажёра.',
  };

  const chatId = String(message.chat.id);
  try {
    const response = await callClaude({
      systemPrompt: `Ты Кеша — стажёр-бот в Telegram-канале. Пишешь живо, по-русски, без официоза. Никаких em-dash (—), никакого markdown. Лаконично — не больше 3-4 предложений.`,
      userMessage: `Контекст поста:\n${postText}\n\nКомментарий Степана: "${message.text}"\n\n${intentInstructions[intent]}`,
      model: 'claude-haiku-4-5-20251001',
      temperature: 0.7,
      maxTokens: 300,
    });

    await sendMessage(chatId, response, { replyToMessageId: message.message_id });
  } catch (err) {
    console.error('[boss] comment reaction error:', err);
  }
}
```

- [ ] **Step 8.2: Add comment routing to the main router**

In the routing block updated in Task 7, add comment reply routing. The full updated block becomes:

```typescript
  const msg = update.message;
  const cq = update.callback_query;

  if (cq) {
    const data = cq.data ?? '';
    if (data.startsWith('digest_')) {
      await handleDigestCallback(cq);
    } else {
      await handleCallback(cq);
    }
  } else if (msg?.text?.match(/^\/digest/)) {
    await handleDigest(msg);
  } else if (msg?.text?.match(/^\/boss/)) {
    await handleCommand(msg);
  } else if (msg && (msg as any).reply_to_message && msg.from.id === 352830345) {
    await handleCommentReply(msg);
  }
```

- [ ] **Step 8.3: Update TelegramMessage type**

In `netlify/functions/kesha-boss-background.mts`, update the `TelegramMessage` interface:

```typescript
interface TelegramMessage {
  message_id: number;
  from: TelegramUser;
  chat: TelegramChat;
  text?: string;
  reply_to_message?: { message_id: number; text?: string };
}
```

Then in `handleCommentReply`, replace the `as any` casts:

```typescript
  const replyToId = message.reply_to_message?.message_id;
  if (!replyToId) return;
  ...
  const postText = message.reply_to_message?.text ?? '';
```

- [ ] **Step 8.4: Run tests**

```bash
npx vitest run
```

Expected: all tests PASS

- [ ] **Step 8.5: Commit**

```bash
git add netlify/functions/kesha-boss-background.mts
git commit -m "feat(boss): add Stepan-only comment reactions with rate limit (M5)"
```

---

## Final Verification

- [ ] **Run full test suite one last time**

```bash
npx vitest run
```

Expected: all tests PASS, zero failures

- [ ] **TypeScript compile check**

```bash
npx tsc --noEmit
```

Expected: no errors

- [ ] **Final commit if clean**

If any type errors were fixed in the previous step:

```bash
git add -p
git commit -m "chore: fix TypeScript types after M3/M4/M5"
```
